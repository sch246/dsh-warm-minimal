diff --git a/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.i18n.yaml b/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.i18n.yaml
index 4480915345..f0704db863 100644
--- a/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.i18n.yaml
+++ b/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write .agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.md
-2026-08-29-model-input-host-provenance.md: 863df9a6ae9ba3b911ef303e2935286af2e4654d
-2026-08-29-model-input-host-provenance.zh.md: f2e6fb14c99091b364b8f8a45e08dba72bbeaa93
+2026-08-29-model-input-host-provenance.md: 83691b5e346ce07d8ffd2fcc8167b274715d6ac5
+2026-08-29-model-input-host-provenance.zh.md: 393107b91104df8ec0465dcff879bc5af4b647bd
diff --git a/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.md b/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.md
index 863df9a6ae..83691b5e34 100644
--- a/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.md
+++ b/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.md
@@ -10,7 +10,7 @@ Host policy needs to identify which configured plugin contributed each model-vis
 
 ## Decision
 
-`dsh-system-prompt` captures a branded `HostSourceId` when each section, context, or schema provider registers. It walks the actual calling Cordis fiber to its owning Loader Entry and combines the entry's full configured path with a stable tree namespace. A file-backed tree derives an opaque namespace from its resolved configuration filename, so a standing preset keeps its sources across process and fiber replacement while two preset files may reuse row ids without collision. Root Loader entries use their root namespace. A registration outside Loader composition receives the single recognizable `UNKNOWN_HOST_SOURCE_ID`; display names and callback names never substitute for configured identity, and source ids do not expose filenames.
+`dsh-system-prompt` captures a branded `HostSourceId` when each section, context, or schema provider registers. It walks the actual calling Cordis fiber to its owning Loader Entry and combines the entry's full configured path with a stable tree namespace. The same resolver accepts an Entry declaration before its fiber activates, so policy owned by a configured tree can classify all declared siblings without depending on plugin startup order. A file-backed tree derives an opaque namespace from its resolved configuration filename, so a standing preset keeps its sources across process and fiber replacement while two preset files may reuse row ids without collision. Root Loader entries use their root namespace. A registration outside Loader composition receives the single recognizable `UNKNOWN_HOST_SOURCE_ID`; display names and callback names never substitute for configured identity, and source ids do not expose filenames.
 
 The registry stores source beside its registration. Scoped merging therefore selects the source and contribution together, including same-name shadows. `dsh-tools` applies the same rule to each tool definition and returns per-schema source ids through its dynamic prompt provider, so one provider invocation can project tools from different owners without collapsing their provenance to the tools service.
 
@@ -22,6 +22,7 @@ Assembly entry objects are keys in a private `WeakMap`. `promptAssemblySourceInv
 
 - Host policy can admit registry contributions by unambiguous live configured-plugin ownership without importing Loader, inspecting Cordis internals, or mutating wire objects.
 - A source id remains a persistent policy key while its file-backed tree keeps the same resolved filename and configured entry ids. Changing either intentionally creates a new source.
+- A configured tree can resolve source ids for inactive siblings before their plugins register contributions; the declaration and a later active context produce the same id.
 - Direct programmatic plugins remain observable as unknown. A future host that needs out-of-Loader identity must add a separate explicit registration identity rather than infer a display or callback name.
 - Provenance follows retained entry identity through a waterfall. Reconstructed entries deliberately lose attribution instead of inheriting it by matching names.
 - Admission predicates compose conjunctively across global and inherited scoped layers, retain independent effect lifetimes, and still observe candidates that another predicate rejects so inventory collection is complete.
diff --git a/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.zh.md b/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.zh.md
index f2e6fb14c9..393107b911 100644
--- a/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.zh.md
+++ b/.agents/notes/implemented/architecture/2026-08-29-model-input-host-provenance.zh.md
@@ -10,7 +10,7 @@ Host 策略需要在决定组装可以保留哪些内容之前，识别每个面
 
 ## Decision
 
-`dsh-system-prompt` 在每个段、上下文或 schema provider 注册时捕获 branded `HostSourceId`。它从实际调用方 Cordis fiber 向上查找所属 Loader Entry，并把 entry 的完整配置 path 与稳定 tree namespace 组合起来。文件树从解析后的配置文件名派生不透明 namespace，因此 standing preset 在进程与 fiber 替换后保持 source，不同 preset 文件也可以无碰撞地复用 row id。根 Loader entry 使用其 root namespace。Loader 组合之外的注册使用唯一且可识别的 `UNKNOWN_HOST_SOURCE_ID`；展示名和 callback 名绝不代替配置身份，source id 也不会暴露文件名。
+`dsh-system-prompt` 在每个段、上下文或 schema provider 注册时捕获 branded `HostSourceId`。它从实际调用方 Cordis fiber 向上查找所属 Loader Entry，并把 entry 的完整配置 path 与稳定 tree namespace 组合起来。同一 resolver 也接受 fiber 激活前的 Entry 声明，因此配置 tree 所属策略可以分类全部已声明 sibling，而不依赖插件启动顺序。文件树从解析后的配置文件名派生不透明 namespace，因此 standing preset 在进程与 fiber 替换后保持 source，不同 preset 文件也可以无碰撞地复用 row id。根 Loader entry 使用其 root namespace。Loader 组合之外的注册使用唯一且可识别的 `UNKNOWN_HOST_SOURCE_ID`；展示名和 callback 名绝不代替配置身份，source id 也不会暴露文件名。
 
 注册表把 source 与注册内容保存在一起。因此作用域合并会同时选择贡献及其 source，包括同名 shadow。`dsh-tools` 对每个工具定义采用同一规则，并通过动态提示词 provider 返回逐 schema source id，所以一次 provider 调用可以投影来自不同 owner 的工具，而不会把它们的 provenance 都压成 tools service。
 
@@ -22,6 +22,7 @@ Assembly 条目对象作为私有 `WeakMap` 的键。`promptAssemblySourceInvent
 
 - Host 策略可以按无歧义的 live 已配置插件 owner 准入注册表贡献，无需导入 Loader、检查 Cordis 内部字段或修改 wire 对象。
 - 文件树保持相同解析文件名与配置 entry id 时，source id 就是持久策略键；主动修改任一项会创建另一个 source。
+- 配置 tree 可以在 sibling 插件注册贡献前解析未激活 sibling 的 source id；声明与其后激活的 context 会生成相同 id。
 - 直接编程挂载的插件保持可观察的 unknown。未来若 Host 需要 Loader 外身份，必须新增独立的显式注册身份，而不能推断展示名或 callback 名称。
 - Provenance 在 waterfall 中跟随被保留的条目身份。重建的条目会有意失去归属，而不会按名称继承。
 - Admission predicate 在全局层和继承的作用域层之间按合取方式组合，并保持独立 effect 生命周期；即使其他 predicate 已拒绝某候选项，它仍会被观察，从而让 inventory 收集完整。
diff --git a/packages/core/system-prompt/README.i18n.yaml b/packages/core/system-prompt/README.i18n.yaml
index a471a44d1f..4c9d8fb1ea 100644
--- a/packages/core/system-prompt/README.i18n.yaml
+++ b/packages/core/system-prompt/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/core/system-prompt/README.md
-README.md: f0f54aa0ab6c9685a360b873e3d365aa3c0fb99f
-README.zh.md: b59cdf8ed53103bae8022e59a2721dc98234f414
+README.md: 33cd885c86faf68a768b3d3f3a99fb260ef74e97
+README.zh.md: f91ed391e35ae2e8c9f2b1f89dd5797a57a397c3
diff --git a/packages/core/system-prompt/README.md b/packages/core/system-prompt/README.md
index f0f54aa0ab..33cd885c86 100644
--- a/packages/core/system-prompt/README.md
+++ b/packages/core/system-prompt/README.md
@@ -26,6 +26,7 @@ System prompt assembly registry. Plugins contribute ordered sections, tool schem
 
 <!-- @meta-intent:begin dsh-warm-minimal (purpose: document the non-wire host provenance query) -->
 - `hostSourceId(ctx): HostSourceId` resolves the calling Cordis context to the owning entry's stable tree namespace and full Loader-tree path. File-backed trees derive an opaque namespace from the resolved configuration filename, so the same preset survives process and fiber replacement while different presets may reuse row ids safely; nested subtrees include their outer entry path. Registrations outside Loader composition receive `UNKNOWN_HOST_SOURCE_ID`; `isUnknownHostSourceId()` distinguishes that case without interpreting the string.
+- `hostSourceIdForEntry(entry): HostSourceId` resolves the same identity directly from a Loader entry declaration. The entry need not have an active fiber, so tree-owned policy can classify every declared sibling during startup without depending on plugin activation order.
 - `promptAssemblySourceInventory(assembly): PromptAssemblySourceInventory` returns the source of each effective section, context, and tool schema in current assembly order. Scoped shadows report the winning registration, dynamic tool schemas are attributed individually, and listener-created or copied entries report the unknown source. Provenance is stored outside assembly objects: it is absent from object enumeration and JSON serialization, while spreading the assembly preserves inventory because its entry objects remain the same.
 - `ctx.systemPrompt.admitSources(predicate: ModelSourceAdmissionPredicate): () => void` registers source admission for the calling scope. Every effective global and inherited scoped predicate observes every effective registry candidate as `(AssembleContext, { kind, name, source })`; all predicates must accept before a section, context, or tool schema enters assembly. Sections are filtered before complete-section selection and text evaluation, contexts before text evaluation, and dynamic tools schema by schema using each schema's effective source. Disposal removes that exact predicate. Waterfall listeners own entries they create because registry admission has already completed.
 <!-- @meta-intent:end dsh-warm-minimal -->
diff --git a/packages/core/system-prompt/README.zh.md b/packages/core/system-prompt/README.zh.md
index b59cdf8ed5..f91ed391e3 100644
--- a/packages/core/system-prompt/README.zh.md
+++ b/packages/core/system-prompt/README.zh.md
@@ -26,6 +26,7 @@
 
 <!-- @meta-intent:begin dsh-warm-minimal (purpose: document the non-wire host provenance query) -->
 - `hostSourceId(ctx): HostSourceId`：把调用方 Cordis context 解析为所属 entry 的稳定 tree namespace 与完整 Loader-tree path。文件树从解析后的配置文件名派生不透明 namespace，因此同一 preset 在进程或 fiber 替换后保持不变，不同 preset 则可安全复用 row id；嵌套 subtree 会包含外层 entry path。Loader 组合之外的注册使用 `UNKNOWN_HOST_SOURCE_ID`；`isUnknownHostSourceId()` 无需解释字符串即可识别该情况。
+- `hostSourceIdForEntry(entry): HostSourceId`：直接从 Loader entry 声明解析相同身份。该 entry 无需拥有活动 fiber，因此 tree 所属策略可以在启动期间分类全部已声明 sibling，而不依赖插件激活顺序。
 - `promptAssemblySourceInventory(assembly): PromptAssemblySourceInventory`：按当前组装顺序返回每个有效段、上下文和工具 schema 的来源。作用域遮蔽报告胜出的注册，动态工具 schema 逐项归属，监听器新建或复制的条目报告 unknown 来源。Provenance 保存在组装对象之外：对象枚举和 JSON 序列化都不含它；assembly spread 保留条目对象身份，因此 inventory 仍可查询。
 - `ctx.systemPrompt.admitSources(predicate: ModelSourceAdmissionPredicate): () => void`：为调用作用域注册 source admission。每个有效的全局和继承的作用域 predicate 都会以 `(AssembleContext, { kind, name, source })` 观察每个有效注册表候选项；只有全部 predicate 接受，段、上下文或工具 schema 才会进入组装。段在选择 complete 段和求值文本前筛选；上下文在求值文本前筛选；动态工具则按每个 schema 的有效 source 逐项筛选。Dispose 会移除该确切 predicate。注册表准入已经完成，因此 waterfall 监听器对其新建条目负责。
 <!-- @meta-intent:end dsh-warm-minimal -->
diff --git a/packages/core/system-prompt/src/index.ts b/packages/core/system-prompt/src/index.ts
index 168b6caf51..55f1ef1f3d 100644
--- a/packages/core/system-prompt/src/index.ts
+++ b/packages/core/system-prompt/src/index.ts
@@ -19,23 +19,33 @@ export type HostSourceId = Branded<'HostSourceId'>
 /** Source used when a registration has no owning Cordis Loader entry instance. */
 export const UNKNOWN_HOST_SOURCE_ID = 'host-source:unknown' as HostSourceId
 
-interface LoaderEntry {
+interface LoaderEntryIdentity {
   readonly id: string
   readonly parent?: { readonly tree?: { readonly filename?: string } }
 }
 
 interface EntryFiber {
-  readonly entry?: LoaderEntry
+  readonly entry?: LoaderEntryIdentity
   readonly parent: { readonly fiber: EntryFiber }
 }
 
 /** Stable namespace for a configured entry tree without exposing its filename. */
-function entryTreeNamespace(entry: LoaderEntry): string {
+function entryTreeNamespace(entry: LoaderEntryIdentity): string {
   const filename = entry.parent?.tree?.filename
   if (filename === undefined) return 'root'
   return `file-${createHash('sha256').update(filename).digest('hex')}`
 }
 
+/**
+ * Resolve a configured Loader entry declaration to its stable source identity.
+ * The entry does not need an active fiber, so an owner may classify sibling declarations during tree startup.
+ * @param entry - Loader entry declaration with its full id and owning tree.
+ * @returns its stable tree namespace and full entry path.
+ */
+export function hostSourceIdForEntry(entry: LoaderEntryIdentity): HostSourceId {
+  return `loader-entry:${entryTreeNamespace(entry)}:${entry.id}` as HostSourceId
+}
+
 /**
  * Resolve the configured Loader entry that owns the calling Cordis context.
  * @param ctx - registration context whose active fiber owns the contribution.
@@ -45,7 +55,7 @@ export function hostSourceId(ctx: Context): HostSourceId {
   let fiber = ctx.fiber as unknown as EntryFiber
   while (true) {
     if (fiber.entry !== undefined) {
-      return `loader-entry:${entryTreeNamespace(fiber.entry)}:${fiber.entry.id}` as HostSourceId
+      return hostSourceIdForEntry(fiber.entry)
     }
     const parent = fiber.parent.fiber
     if (fiber === parent) return UNKNOWN_HOST_SOURCE_ID
diff --git a/packages/core/system-prompt/tests/provenance.spec.ts b/packages/core/system-prompt/tests/provenance.spec.ts
index fc6e752e6f..f03abe28e7 100644
--- a/packages/core/system-prompt/tests/provenance.spec.ts
+++ b/packages/core/system-prompt/tests/provenance.spec.ts
@@ -5,6 +5,7 @@ import { createScope } from '@deepseek-ai/dsh-scope'
 import SystemPrompt, {
   UNKNOWN_HOST_SOURCE_ID,
   hostSourceId,
+  hostSourceIdForEntry,
   isUnknownHostSourceId,
   promptAssemblySourceInventory,
 } from '@deepseek-ai/dsh-system-prompt'
@@ -36,6 +37,18 @@ function sourceOf(assembly: PromptAssembly, kind: 'sections' | 'contexts' | 'too
 }
 
 describe('prompt assembly host provenance', () => {
+  it('derives the same source before and after a Loader entry mounts', () => {
+    const entry = { id: 'nested:tool', parent: { tree: { filename: '/presets/warm-minimal.cordis.yml' } } }
+    const mounted = hostSourceId({
+      fiber: {
+        entry,
+        parent: { fiber: undefined },
+      },
+    } as unknown as Context)
+
+    expect(hostSourceIdForEntry(entry)).toBe(mounted)
+  })
+
   it('separates direct file-backed trees while keeping each configured path stable', () => {
     const source = (filename: string): HostSourceId => hostSourceId({
       fiber: {
