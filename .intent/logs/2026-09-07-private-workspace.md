# Private maintenance workspace

Date: 2026-09-07

The requested layout separates the repository's maintenance authority from the installable npm package. Runtime modules, source, generated output, presets, Bundle metadata and build configuration moved into `packages/dsh-warm-minimal`; root `.intent/`, instructions, documentation, Host patch and scripts remain repository-owned. Package version 0.2.0 and persistent/runtime identities were preserved. Pure runtime and component tests now resolve peers from the package; repository lifecycle tests retain their root fixture and patch ownership.

Default setup/removal now inspect, with `--install` and `--remove` selecting mutations. Operations require explicit checkout, Home and profile and call the selected checkout's built CLI. PowerShell wrappers share the Node entry and retained Bash implementation. The Host revision guard, exact forward/reverse patch checks, preset ownership checks, explicit replacement overrides and refusal on drift remain enforced. The supported revision is owned by `scripts/host-revision`. This migration does not establish current Harness HEAD installation support or select a realization lock.

The previous lifecycle fixture assembled files from an older baseline and committed an arbitrary HEAD, which could not satisfy the installer's pinned revision. Its replacement uses a private sparse clone at the real pinned commit, a private Home and a built-CLI stub. These tests exercise real patch application/removal and preserve the production revision guard; they do not test a live profile transaction.

Validation used existing read-only dependency package links for TypeScript 5.9.3 and tsdown 0.22.14. The workspace pins pnpm 10.17.1; tests use Node's existing runner and need no Vitest dependency. Host/Client build and typecheck passed against `/root/deepseek-harness` artifacts. The focused suite passed all 38 cases, including exact legacy preset upgrade, unknown ownership, patch/preset drift, default inspection, required environment, unsupported revision and CLI removal failure. No profile installation, service restart, provider/browser acceptance, Windows execution or publication was performed. Setup still spans non-atomic Host/preset/profile operations, and failed registration can leave earlier writes for inspection.

A final package-test run exposed that the old root UI-primitives fixture resolved React through root-only preparation. Moving that fixture alongside the package tests removed the root dependency resolution requirement. Re-running the final 38-case suite passed. A borrowed tool alias moved during concurrent workspace maintenance; validation resumed through the exact existing dependency package paths. The final Host generator build passed with locally owned dependency directories.

The STATE review separated adaptation requirements from implementation observations. The recorded implementation admits system-prompt/context and tool schemas but does not role-filter durable AGENTS user messages, leaving WARM-006/009 acceptance incomplete. Its stdio LSP provider has no sandbox confinement and remains outside the worker-safe roster. These observations do not narrow the intended source-family coverage or language-tooling requirements. Earlier donor evidence also implicated reasoning effort; the exact initial prompt and two-tool face remain user-selected.

Earlier records report an installed alpha.2 candidate and a subsequent persona-placement API repair. Local Web observations and stale locks prove only their recorded targets; they do not establish the current deployment or complete acceptance. Full cold-browser, three-phase request, actual delegation, persistence/resume and uninstall acceptance remain pending. This documentation follow-up checked no live target and repeated no runtime build or tests.
