diff --git a/packages/client/ui-conversation/src/client/conversation-nodes/common.ts b/packages/client/ui-conversation/src/client/conversation-nodes/common.ts
index 1e2693bfff..6ac77bbac4 100644
--- a/packages/client/ui-conversation/src/client/conversation-nodes/common.ts
+++ b/packages/client/ui-conversation/src/client/conversation-nodes/common.ts
@@ -46,14 +46,17 @@ export function chatNode<Kind extends ChatNodeKind>(
     readonly visibility?: 'visible' | 'hidden'
   } = {},
 ): ChatNode<Kind> {
+  const location = options.location ?? contextLocation(context)
+  const hideFromChat = (location.kind === 'turn' || location.kind === 'step')
+    && location.turn.data.get('input-message')?.hideFromChat === true
   return {
     key: context.key,
     kind,
     id: context.id,
     target: 'chat',
     anchorSeq,
-    location: options.location ?? contextLocation(context),
-    visibility: options.visibility ?? 'visible',
+    location,
+    visibility: hideFromChat ? 'hidden' : options.visibility ?? 'visible',
     data,
   }
 }
diff --git a/packages/client/ui-conversation/src/client/conversation-nodes/message.ts b/packages/client/ui-conversation/src/client/conversation-nodes/message.ts
index c60bbf79f1..5a7f7795de 100644
--- a/packages/client/ui-conversation/src/client/conversation-nodes/message.ts
+++ b/packages/client/ui-conversation/src/client/conversation-nodes/message.ts
@@ -31,6 +31,20 @@ declare module '@deepseek-ai/dsh-client-ui-conversation/client' {
   }
 }
 
+declare module '@deepseek-ai/dsh-client-runtime/client' {
+  interface ConversationTurnDataMap {
+    /** A model-visible synthetic warmup turn omitted from ordinary Chat. */
+    'input-message': { readonly hideFromChat: true }
+  }
+}
+
+function isWarmupSource(source: unknown): boolean {
+  return typeof source === 'object'
+    && source !== null
+    && 'form' in source
+    && source.form === 'warmup'
+}
+
 function isCompactionCheckpoint(event: Parameters<ConversationNodeDefinition['match']>[0]): boolean {
   if (event.type !== 'user/message' || !isReplacementSurfaceEvent(event)) return false
   const source = event.data.source
@@ -79,6 +93,18 @@ export const messageDefinition: ConversationNodeDefinition<MessageNode> = {
       }
   },
   update: context => context.state,
+  buildLocationData: (context, scope) => {
+    if (scope !== 'turn'
+      || context.state?.kind !== 'context'
+      || !isWarmupSource(context.state.source)) return null
+    const location = context.start?.location
+    const turn = location?.kind === 'turn' || location?.kind === 'step'
+      ? location.turn.turn
+      : undefined
+    return turn === undefined
+      ? null
+      : { kind: 'turn', turn, key: 'input-message', value: { hideFromChat: true } }
+  },
   buildViewNode: (context) => {
     if (context.state === undefined) return null
     return chatNode(context, context.state.kind, context.state.seq, context.state)
diff --git a/packages/client/ui-trajectory/src/client/layout.ts b/packages/client/ui-trajectory/src/client/layout.ts
index 265d5ba034..a21b334a2b 100644
--- a/packages/client/ui-trajectory/src/client/layout.ts
+++ b/packages/client/ui-trajectory/src/client/layout.ts
@@ -279,7 +279,7 @@ export function deriveTrajectoryLayout(input: TrajectoryLayoutInput): readonly T
     if (entry.kind === 'system') {
       const { change, request } = entry
       const turn = change.kind === 'initial'
-        ? firstVisibleTurn(nodes, partial)
+        ? request.turn
         : enclosingPromptTurn(nodes, change.seq, partial)
       pushMessage(turn, {
         absTime: finiteTime(change.time),
@@ -352,9 +352,9 @@ export function deriveTrajectoryLayout(input: TrajectoryLayoutInput): readonly T
     }
     const { node, nodeIndex: i } = entry
     if (node.kind === 'user') {
-      // user/message has no turn on the wire; enclose it in the next assistant
-      // (or partial) turn, else open the turn after the last assistant.
-      const turn = enclosingUserTurn(followingAssistants[i], partial, lastAssistantTurn)
+      const turn = enclosingInputTurn(
+        eventLocations?.get(node.seq), followingAssistants[i], partial, lastAssistantTurn,
+      )
       pushMessage(turn, {
         absTime: finiteTime(node.time),
         cell: {
@@ -400,7 +400,9 @@ export function deriveTrajectoryLayout(input: TrajectoryLayoutInput): readonly T
       continue
     }
     if (node.kind === 'context') {
-      const turn = enclosingUserTurn(followingAssistants[i], partial, lastAssistantTurn)
+      const turn = enclosingInputTurn(
+        eventLocations?.get(node.seq), followingAssistants[i], partial, lastAssistantTurn,
+      )
       pushMessage(turn, {
         absTime: finiteTime(node.time),
         cell: {
@@ -607,10 +609,16 @@ function toTurnModel(
   return { turn, groups }
 }
 
-/** Chronological section position from the fold's monotonically assigned cell indexes. */
+/**
+ * Chronological section position from durable cells. An initial system prompt
+ * is globally folded first so it leads inside its request turn, but it must not
+ * pull that whole turn ahead of an earlier durable turn.
+ */
 function firstCellIndex(turn: TrajectoryTurnModel): number {
+  const cells = turn.groups.flatMap(group => group.cells)
+  const durable = cells.filter(cell => cell.kind !== 'system')
   return Math.min(
-    ...turn.groups.flatMap(group => group.cells.map(cell => cell.index)),
+    ...(durable.length > 0 ? durable : cells).map(cell => cell.index),
     Number.POSITIVE_INFINITY,
   )
 }
@@ -867,6 +875,17 @@ function enclosingUserTurn(
   return 1
 }
 
+/** Prefer the durable location; adjacency inference is only a history fallback. */
+function enclosingInputTurn(
+  location: ConversationLocation | undefined,
+  followingAssistant: AssistantMessageNode | undefined,
+  partial: ConversationSnapshot['partial'],
+  lastAssistantTurn: number | null,
+): number {
+  if (location?.kind === 'turn' || location?.kind === 'step') return location.turn.turn
+  return enclosingUserTurn(followingAssistant, partial, lastAssistantTurn)
+}
+
 function steeringPlacement(
   followingAssistant: AssistantMessageNode | undefined,
   partial: ConversationSnapshot['partial'],
@@ -915,20 +934,6 @@ function enclosingPromptTurn(
   return partial?.turn ?? 1
 }
 
-/** Earliest raw turn represented by the selected trajectory branch. */
-function firstVisibleTurn(
-  nodes: ConversationSnapshot['nodes'],
-  partial: ConversationSnapshot['partial'],
-): number {
-  const turns = nodes.flatMap(node =>
-    node.kind === 'assistant' && node.turn > 0
-      ? [node.turn]
-      : [],
-  )
-  if (partial !== null && partial.turn > 0) turns.push(partial.turn)
-  return turns.length === 0 ? 1 : Math.min(...turns)
-}
-
 /** Copy provider usage onto a Message cell when present. */
 function attachUsage(cell: TrajectoryCellProps, usage: UsageLike | undefined): void {
   if (usage === undefined) return
diff --git a/packages/extensions/tool-cordis/src/api-catalog.ts b/packages/extensions/tool-cordis/src/api-catalog.ts
index 6170be9695..6f91bf9d59 100644
--- a/packages/extensions/tool-cordis/src/api-catalog.ts
+++ b/packages/extensions/tool-cordis/src/api-catalog.ts
@@ -3134,7 +3134,7 @@ export const TYPE_API: readonly TypeApiEntry[] = [
   },
   {
     name: 'ContextFormed',
-    declaration: 'export type ContextFormed = {\n    readonly form?: never;\n} | {\n    readonly form: \'instructions\';\n} | {\n    readonly form: \'catalog\';\n} | {\n    readonly form: \'snapshot\';\n    readonly sections: readonly ContextSnapshotSection[];\n} | {\n    readonly form: \'notice\';\n    readonly summary: string;\n} | {\n    readonly form: \'relay\';\n} | {\n    readonly form: \'recall\';\n};',
+    declaration: 'export type ContextFormed = {\n    readonly form?: never;\n} | {\n    readonly form: \'warmup\';\n} | {\n    readonly form: \'instructions\';\n} | {\n    readonly form: \'catalog\';\n} | {\n    readonly form: \'snapshot\';\n    readonly sections: readonly ContextSnapshotSection[];\n} | {\n    readonly form: \'notice\';\n    readonly summary: string;\n} | {\n    readonly form: \'relay\';\n} | {\n    readonly form: \'recall\';\n};',
   },
   {
     name: 'ContextSnapshotSection',
diff --git a/packages/llm/llm/src/message.ts b/packages/llm/llm/src/message.ts
index 608b56475d..e9071045f0 100644
--- a/packages/llm/llm/src/message.ts
+++ b/packages/llm/llm/src/message.ts
@@ -46,6 +46,8 @@ export interface ToolMessageSource {
  * unknown value is the documented default, presented as opaque content.
  */
 export type ContextForm =
+  /** A synthetic prior that prepares the model before it handles the first real request. */
+  | 'warmup'
   /** Instructions read out of workspace files the model is expected to follow. */
   | 'instructions'
   /** A catalog of items available in this session, republished as it changes. */
@@ -78,6 +80,7 @@ export interface ContextSnapshotSection {
  */
 export type ContextFormed =
   | { readonly form?: never }
+  | { readonly form: 'warmup' }
   | { readonly form: 'instructions' }
   | { readonly form: 'catalog' }
   | {
